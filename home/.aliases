alias please='sudo'

# Quickly navigate up the *great hollow*
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'
alias .....='cd ../../../..'

# Directory shortcuts
alias db='cd ${HOME}/Dropbox'
alias dl='cd ${HOME}/Downloads'
alias dt='cd ${HOME}/Desktop'
alias rp='cd ${HOME}/dev/repos'
alias mi='cd ${HOME}/Dropbox/mi'

# Weather
# Found in @noopkat’s dotfiles (https://github.com/noopkat/dotfiles)
alias weather='curl http://wttr.in/Eindhoven'
alias moon='curl http://wttr.in/Moon'

# Extract .tar.gz archives because I don’t want to remember the required flags
alias untargz='tar -xf'

# I finally had enough of typing this out
alias aptupdate='sudo apt update \
  && apt list --upgradable \
  && sudo apt upgrade -y \
  && sudo snap refresh'

# Echo path, but with each entry on its own line
alias path='echo -e ${PATH//:/\\n}'

# Open files in an editor like code, subl, gedit or vim.
alias o=code

# https://github.com/sharkdp/bat
alias cat='bat --plain'

# https://github.com/sharkdp/fd
alias fd='fdfind'



# Enable colored output and sort output by directory first, files second,
# symlinks third

# Default ls should always show
# -l  as a list
# -h  with human-readable file sizes
# -F  with indicator (e.g. trailing / for directories)
alias ls='ls -lhF --color'

list_sorted() {
  # List directories
  ls $* | grep "^d" | awk '{printf "\033[1;34m📁\033[0m " $0 "\n"}';
  # List regular files
  ls $* | grep "^-" | awk '{print "📄 " $0}';
  # List links
  ls $* | grep "^l" | awk '{print "🔗 " $0}';
  # List everything else (e.g. symbolic links)
  ls $* | grep -v -E "^d|^-|^l|^total"
}

alias ll=list_sorted

alias tree="tree -I '.git|node_modules'"

alias python=python3
alias pip=pip3

alias deploy_kleinfreund='rsync -av --delete ${HOME}/dev/repos/kleinfreund.de/_site/ phr@brorsen.uberspace.de:/var/www/virtual/phr/kleinfreund.de'
